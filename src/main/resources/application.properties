# Set the server port (default is 8080)
server.port=9000

# (Optional) Set the application context path
# server.servlet.context-path=/calculator

# (Optional) Set the application name
spring.application.name=CalculatorApp

